apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-project-1.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Values.app }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}

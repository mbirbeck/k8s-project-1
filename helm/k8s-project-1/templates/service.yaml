apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-project-1.fullname" . }}
  labels:
    {{- include "k8s-project-1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "k8s-project-1.selectorLabels" . | nindent 4 }}
